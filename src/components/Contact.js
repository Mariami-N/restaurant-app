import styled from "styled-components"
const Title = styled.h1`
     color: gold;
     `

export default function Contact () {
    return (
        <>
        <Title>contact</Title>
        </>
    )
    }